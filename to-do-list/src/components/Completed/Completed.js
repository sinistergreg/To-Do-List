import React from 'react';

function Completed() {
  return (
    <div>
      <h2>Completed Tasks</h2>
      {/* You can map over your completed tasks and display them here */}
      {/* For example: */}
      {/* {completedTasks.map(task => <div key={task.id}>{task.title}</div>)} */}
      {/* Replace completedTasks with your actual completed tasks array */}
      <p>Display your completed tasks here...</p>
    </div>
  );
}

export default Completed;